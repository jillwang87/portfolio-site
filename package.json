{
  "name": "portfolio-site",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "classnames": "^2.2.6",
    "node-sass": "4.14.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-flippy": "^0.1.5",
    "react-router-dom": "^5.2.0",
    "react-scripts": "4.0.2",
    "use-mobile-detect-hook": "^1.0.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build && cp 404.html build/404.html && touch build/.nojekyll",
    "deploy": "gh-pages --dotfiles --repo git@github.com:jillwang87/jillwang87.github.io.git -b master -d build",
    "clean": "gh-pages-clean"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "homepage": "https://jillwang87.github.io/",
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
